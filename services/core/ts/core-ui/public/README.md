Static assets in this directory are copied as-is to the site root at build
time (see `angular.json`'s `assets` glob).

Drop the mascot image here as `darth-vader.png` — it's referenced by
`src/app/app.html` and rendered next to the "VADER" wordmark in the header.
